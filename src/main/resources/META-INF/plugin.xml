<idea-plugin>
    <id>com.simple.SimpleConvert</id>

    <name>SimpleConvert</name>

    <vendor email="tangpengzy@163.com">simple</vendor>

    <description><![CDATA[
      用于实体装换插件,支持ArrayList。
      <br/>
      <br/>


      <br/>
      <b>介绍</b>
      <ul>
          <li>Just define a good method to enter and leave the rest to the plugin!
The default shortcut key for MacOs is command + shift + L
The default shortcut keys for Windows are ctrl, shift and L
Move the cursor to the corresponding method body and press the shortcut key to start</li>
      </ul>

    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.lang</depends>
    <depends>com.intellij.java</depends>
    <depends>com.intellij.modules.java</depends>

    <actions>
        <action id="ConvertAction" class="com.simple.demo5.ConvertAction"
                text="Simple" description="Simple plugin">
            <add-to-group group-id="GenerateGroup" anchor="last"/>
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl shift L"/>
        </action>
    </actions>
</idea-plugin>